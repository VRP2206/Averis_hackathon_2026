{
  "name": "shipdoc-docs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Documentation site for shipdoc (docs.shipdoc.org)",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "npm run sync && vitepress dev",
    "build": "npm run sync && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "mermaid": "^11.4.0",
    "vitepress": "^1.6.3",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
